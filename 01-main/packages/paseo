DEFVER=1
get_github_releases "getpaseo/paseo" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*Paseo-.*-${HOST_ARCH}\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL/v/}")
fi
PRETTY_NAME="Paseo"
WEBSITE="https://paseo.sh/"
SUMMARY="Orchestrate multiple coding agents from desktop and mobile."
